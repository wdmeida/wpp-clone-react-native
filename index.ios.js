import React from 'react';
import { AppRegistry } from 'react-native';

import App from './src/App'

const wpp_clone = props => (
  <App />
)

AppRegistry.registerComponent('wpp_clone', () => wpp_clone);
